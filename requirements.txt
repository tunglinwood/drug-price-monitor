# Streamlit Cloud 专用依赖 (Python 3.14 兼容)

# 核心依赖 - 不指定版本，让 Streamlit 自动选择
streamlit
pandas
pubchempy
python-dotenv

# 认证和权限
streamlit-authenticator>=0.3.3
bcrypt>=4.0.0
extra-streamlit-components>=0.1.60
